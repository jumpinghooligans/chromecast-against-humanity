extends ../layout

block content
	h2 Users
	ul#userList
		each user in response.users
			li: a(href='/users/' + user.username) #{user.username}
	a(href='/users/create', class='btn btn-default') Create User