extends ../layout

block content
	h2 Games
	ul
		each game in response.games
			li: a(href='/game/' + game.name) #{game.name}
			em Creator: <strong>#{game.creator}</strong>
	a(href='/game/create') Create Game